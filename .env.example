# Example environment for docker-compose
# Copy to .env and fill in as needed. Secrets should go into ./secrets files instead.

# Comma or semicolon separated list of channel IDs
ALLOWED_CHANNEL_IDS=1351265799561809920

# Save cropped icons to disk during parse
SAVE_ICONS=false

# How far back to scan on startup
INITIAL_RECENT_HOURS=48

# How far back to scan on each recurring pass
RECURRING_RECENT_HOURS=1

# Optional: override spreadsheet name
GOOGLE_SPREADSHEET_NAME=AlphaBot Match Data v1
